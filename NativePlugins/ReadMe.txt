Example showing the usage of simple native plugins on Windows Store Apps

* Open the project with Unity 5.3 or higher
* Build to Windows Store (SDK 8.1)
* Run
* You should see "Get Integer returns 5"

The source for native plugin is located in "WSANativePlugins\Source", it contains both Editor and WSA plugin projects